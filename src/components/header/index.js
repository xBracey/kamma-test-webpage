import React from "react"
import { HeaderContainer, Logo } from "./styles"
import logo from "../../../static/logo.svg"

export default () => (
  <HeaderContainer>
    <Logo src={logo} />
  </HeaderContainer>
)
